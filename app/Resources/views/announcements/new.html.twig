{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tworzenie ogłoszenia</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-outline-secondary" type="submit" value="Stwórz" />
        <a class="btn btn-outline-secondary" href="{{ path('announcements_index') }}">Powrót do listy</a>
    {{ form_end(form) }}

{% endblock %}
